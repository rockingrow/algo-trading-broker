info:
  name: Crypto Max Leverage
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/admin/settings/crypto-max-leverage"
  body:
    type: json
    data: |-
      {
        "default_leverage": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
