info:
  name: Toggle BlockSignal
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/admin/settings/block-signal"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
