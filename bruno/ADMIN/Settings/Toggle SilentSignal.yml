info:
  name: Toggle SilentSignal
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/admin/settings/silent-signal"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
