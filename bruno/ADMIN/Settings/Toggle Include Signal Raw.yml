info:
  name: Toggle Include Signal Raw
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/admin/settings/include-signal-raw"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
