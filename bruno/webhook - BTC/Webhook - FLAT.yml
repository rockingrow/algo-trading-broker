info:
  name: Webhook - FLAT
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/webhook"
  body:
    type: json
    data: |-
      {
        "strategy": "MT5_BTC_M5_V1",
        "token": "{{webhook_token}}",
        "symbol": "OANDA:BTCUSD",
        "timeframe": "5",
        "timestamp": "2026-04-25 20:03:00",
        "position": {
          "action": "FLAT"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
