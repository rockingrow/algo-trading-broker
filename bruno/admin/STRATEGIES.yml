info:
  name: STRATEGIES
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/admin/strategies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
