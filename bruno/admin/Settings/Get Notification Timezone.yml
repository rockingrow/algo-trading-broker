info:
  name: Get Notification Timezone
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/admin/settings/notification-timezone"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
