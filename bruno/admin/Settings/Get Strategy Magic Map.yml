info:
  name: Get Strategy Magic Map
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/admin/settings/strategy-magic-map"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
