info:
  name: Set Notification Timezone
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/admin/settings/notification-timezone"
  body:
    type: json
    data: |-
      {
        "utc_offset_hours": 7
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
