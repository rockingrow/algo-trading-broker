info:
  name: Rotate Link Token
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/admin/accounts/{{process.env.account_id}}/link-token/rotate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
