info:
  name: Toggle Prevent Signal
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/settings/prevent-signal"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
