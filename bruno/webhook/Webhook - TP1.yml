info:
  name: Webhook - TP1
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/secret/webhook"
  body:
    type: json
    data: |-
      {
        "strategy": "{{process.env.strategy}}",
        "token": "{{webhook_token}}",
        "symbol": "OANDA:{{process.env.symbol}}",
        "timeframe": "5",
        "timestamp": "2026-04-22 03:50:00",
        "signal_uxid": "9f2c4b7e18a3d008",
        "position": {
          "action": "TP1",
          "price": 63500.50,
          "quantity": 0.01,
          "risk_percent": 3.0,
          "is_running": true,
          "tp1_percent": 70.0,
          "use_equity_sizing": true,
          "move_sl_to_be": false
        },
        "indicators": {},
        "inputs": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
