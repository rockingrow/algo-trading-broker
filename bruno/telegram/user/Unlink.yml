info:
  name: Unlink
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/v1/telegram/{{process.env.telegram_user_id}}/unlink"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
