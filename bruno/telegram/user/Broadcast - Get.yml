info:
  name: Broadcast - Get
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/v1/telegram/{{process.env.telegram_user_id}}/broadcast"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
