info:
  name: List Accounts
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/v1/telegram/{{process.env.telegram_user_id}}/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
