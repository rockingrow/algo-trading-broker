info:
  name: Broadcast - Subscribe
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/v1/telegram/{{process.env.telegram_user_id}}/broadcast/subscribe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
