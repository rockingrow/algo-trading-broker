info:
  name: Broadcast - Unsubscribe
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/v1/telegram/{{process.env.telegram_user_id}}/broadcast/unsubscribe"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
